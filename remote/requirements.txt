# Hosted server only — smaller than the root requirements.txt, which also
# carries Flask and Gemini for the standalone desktop modes.
fastmcp>=3.0.0
google-api-python-client>=2.108.0
google-auth>=2.0.0
dnspython>=2.6.0
httpx>=0.27.0

# Starlette needs this to parse multipart form data — without it the file
# upload route raises at request.form() rather than at import, so it fails
# only in production. Pinned in explicitly for that reason.
python-multipart
python-dotenv>=1.0.0
pydantic>=2.0.0

# Postgres (Neon/Supabase/Railway). Only used when DATABASE_URL is set;
# without it storage falls back to SQLite from the stdlib.
psycopg[binary]>=3.1.0
