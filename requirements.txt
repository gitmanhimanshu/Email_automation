# Gmail API — all modes
google-api-python-client>=2.108.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0

# MCP server (Claude writes the emails)
mcp>=1.2.0
pydantic>=2.0.0

# Web interface
flask>=3.0.0

# Gemini — standalone CLI/web mode only, not used by the MCP server
google-generativeai>=0.8.0

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0
